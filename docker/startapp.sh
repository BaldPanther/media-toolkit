#!/bin/sh
# Запуск приложения внутри контейнера — его вызывает базовый образ.
# /media — стартовая папка, пока в настройках нет последней открытой.
cd /app || exit 1
exec /opt/venv/bin/python /app/app.py /media
