# Python
__pycache__/
*.pyc

# Машинно-зависимый ярлык (воссоздаётся make_shortcut.ps1)
*.lnk

# Настройки (ключи TMDb/Fanart.tv/OMDb, аккаунт OpenSubtitles, последний путь)
# переехали в пользовательский каталог ОС — см. paths.py. Правило оставлено
# страховкой: на старых копиях эти файлы ещё лежат здесь, плюс так же будет
# прикрыт любой будущий *_settings.json.
*settings.json

# Личные заметки — отдельный приватный репозиторий media-toolkit-notes,
# склонированный в notes/. В этот (публичный) репозиторий не попадают.
notes/

# Портативный Chromaprint для Windows: чужой бинарник, в репозиторий не кладём.
# Кто запускает из исходников — скачивает сам (см. README, «Требования для автодетекта»).
assets/fpcalc.exe

.pytest_cache/

# Данные локального запуска docker compose (настройки контейнера, тестовая медиатека).
config/
media/
.env
