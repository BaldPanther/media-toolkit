# Python 3.14, macOS и Windows.
# Базовая работа (смена дорожек по умолчанию) зависимостей не требует —
# нужны только Python 3 с tkinter и MKVToolNix (mkvmerge/mkvpropedit).
#
# Внешние инструменты (не через pip):
#   MKVToolNix  mac: brew install mkvtoolnix    win: choco install mkvtoolnix
#   ffmpeg      mac: brew install ffmpeg        win: Gyan.FFmpeg через winget
#   fpcalc      mac: brew install chromaprint   win: портативный в assets/fpcalc.exe

# Опционально: для кнопки «Скачать русские субтитры».
subliminal>=2.6

# Опционально: для автодетекта интро/титров (вкладка «Пропуск заставок»).
# Нужны также ffmpeg и fpcalc — см. выше.
numpy>=1.26

# Опционально: вкладка «Медиатека».
#   pillow     — миниатюры в сетке выбора постеров (штатный tkinter не читает JPEG).
#                Без него арт выбирается автоматически, кнопка «Выбрать…» отключена.
#   send2trash — удаление мусора из раздачи в Корзину, а не безвозвратно.
#                Без него галка «удалять мусор» заблокирована, всё уезжает в Extras.
pillow>=10
send2trash>=1.8

# Только для разработки: юнит-тесты (python -m pytest tests/ -q).
# pytest>=8
